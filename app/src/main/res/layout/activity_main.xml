<?xml version="1.0" encoding="utf-8"?>
<com.idiotnation.raspored.Views.CustomSwipeToRefresh xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/refresh_layout"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".Views.MainView">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.Toolbar
            android:id="@+id/action_bar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            android:elevation="4dp"
            android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

        <android.support.design.widget.TabLayout
            android:id="@+id/dayTabs"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_below="@+id/action_bar"
            app:tabBackground="@android:color/transparent"
            app:tabIndicatorColor="@color/colorAccent"
            app:tabIndicatorHeight="5dp"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@color/tabsBarTextColorPrimary"
            app:tabTextColor="@color/tabsBarTextColorSecondary"/>

        <RelativeLayout
            android:id="@+id/rl_1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/dayTabs"
            android:clickable="true">

            <RelativeLayout
                android:id="@+id/sati"
                android:layout_width="130dp"
                android:layout_height="match_parent"
                android:layout_alignParentLeft="true" />

            <android.support.v4.view.ViewPager
                android:id="@+id/pager"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_alignParentRight="true"
                android:layout_toEndOf="@+id/sati"
                android:layout_toRightOf="@+id/sati"
                android:overScrollMode="never" />

        </RelativeLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/action_bar">

            <TextView
                android:id="@+id/info_msg"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:padding="15dp"
                android:textColor="@color/textColorPrimary"
                android:background="@color/windowBackgroundColor"
                android:textSize="14dp"
                android:textStyle="bold"
                android:visibility="invisible" />

        </RelativeLayout>

        <FrameLayout
            android:id="@+id/easter_egg_bg"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:background="@drawable/spooderman"
            android:clickable="true"
            android:visibility="gone" />
    </RelativeLayout>
</com.idiotnation.raspored.Views.CustomSwipeToRefresh>
