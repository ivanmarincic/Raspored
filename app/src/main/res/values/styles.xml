<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorOnPrimary</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:colorForeground">@color/colorOnBackground</item>
        <item name="android:textColorPrimaryInverse">@color/colorOnPrimaryInverse</item>
        <item name="android:textColorSecondary">@color/colorOnPrimary</item>
        <item name="android:textColorSecondaryInverse">@color/colorOnPrimaryInverse</item>
    </style>
</resources>
